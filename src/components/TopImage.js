import SearchBar from "./SearchBar";

const TopImage = () => {
  return (
    <div className="topImage" id="tempDesign">
      <div id="banner">
        <SearchBar />
      </div>
    </div>
  );
};

export default TopImage;
